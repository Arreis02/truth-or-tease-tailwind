import { wildPromptData } from '../prompts.jsx';

const Debug = () => {
  return (
    <div className="p-4 text-white bg-black">
      <h1 className="text-2xl font-bold">Wild Topics: {wildPromptData.length}</h1>
      <ul>
        {wildPromptData.map((item, index) => (
          <li key={index}>{item.topic}</li>
        ))}
      </ul>
    </div>
  );
};

export default Debug;
